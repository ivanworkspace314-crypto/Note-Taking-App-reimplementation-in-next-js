import NoteEditor from "../../components/NoteEditor";

export const metadata = {
  title: "Create Note - ThinkBoard",
};

export default function CreateNotePage() {
  return <NoteEditor pageTitle="Create New Note" />;
}
